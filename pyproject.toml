[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "5e2pdf"
version = "0.4.1"
description = "Convert D&D 5e JSON data to beautifully formatted LaTeX/PDF documents"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Sam Sargeant" }]
keywords = [
    "dnd",
    "d&d",
    "dungeons-dragons",
    "5e",
    "latex",
    "pdf",
    "cli",
    "generator",
    "rpg",
    "tabletop",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Games/Entertainment :: Role-Playing",
    "Topic :: Text Processing :: Markup :: LaTeX",
    "Topic :: Utilities",
    "Typing :: Typed",
]
dependencies = [
    "requests>=2.31.0,<3.0.0",
    "colorlog>=6.0.0,<7.0.0",
    "pydantic>=2.11.7,<3.0.0",
    "pydantic-settings>=2.0.0,<3.0.0",
    "typer>=0.9.0,<1.0.0",
    "rich>=14.0.0,<15.0.0",
    "pyyaml>=6.0.0,<7.0.0",
    "jinja2>=3.0.0,<4.0.0",
    "lark>=1.2.2,<2.0.0",
    "diskcache>=5.6.3,<6.0.0",
    "pillow>=11.3.0",
    "aiohttp>=3.12.15",
]

[project.urls]
Homepage = "https://github.com/sargeant/5e2pdf"
Repository = "https://github.com/sargeant/5e2pdf"
"Bug Tracker" = "https://github.com/sargeant/5e2pdf/issues"

[project.scripts]
5e2pdf = "dnd5e.cli.main:app"

[project.optional-dependencies]
docs = [
    "sphinx==8.2.3", # Pin to latest Sphinx version
    "sphinx-autobuild>=2024.0.0", # Modern Awesome theme
    "myst-parser==4.0.1", # Pin to MyST 4.0.1 exactly
    "sphinxcontrib-mermaid>=0.9.2", # Mermaid support
    "sphinx-copybutton>=0.5.2", # Copy buttons for code
    "linkify-it-py>=2.0.0", # For MyST linkify extension
    "packaging",
    "shibuya>=2025.7.24",
    "sphinx-design>=0.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.2,<1.0.0",
    "mypy>=1.0.0,<2.0.0",
    "rg>=0.0.75,<1.0.0",
    "semver>=3.0.4,<4.0.0",
    "pytest>=8.0.0,<9.0.0",
    "pytest-asyncio>=1.1.0,<2.0.0",
    "pytest-cov>=6.0.0,<7.0.0",
    "pytest-xdist>=3.0.0,<4.0.0",
    "pytest-json-report>=1.5.0,<2.0.0",  # JSON test reports for performance monitoring
    "hypothesis>=6.98.0,<7.0.0",  # Property-based testing
    "psutil>=5.9.0,<8.0.0",
    "types-pyyaml>=6.0.12.20250516",
    "types-psutil>=7.0.0.20250601",
    "diskcache-stubs>=5.6.3",
    "certifi>=2025.7.14",
    "coverage>=7.10.1,<8.0.0",
    "starlette>=0.47.2,<1.0.0",
    "pip-audit>=2.6.0,<3.0.0",  # Alternative to safety - compatible with Pydantic 2.11+
    # "safety>=3.6.0,<4.0.0",  # Disabled: requires pydantic<2.10.0
    "bandit>=1.8.6,<2.0.0",
]
docs = [
    "shibuya>=2025.7.24",
]

images = ["Pillow>=9.0.0"]
xml = ["lxml>=4.9.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/dnd5e"]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = [
    "A",  # flake8-builtins
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "UP", # pyupgrade
    # "ASYNC", # asyncio-related checks
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    # "S",     # flake8-bandit
    # "FBT",   # flake8-boolean-trap
    # "N",     # pep8-naming
    # "DTZ",   # flake8-datetimez
    # "EXE",   # flake8-executable
    # "ISC",   # flake8-implicit-str-concat
    # "LOG",   # flake8-logging
    # "G",     # flake8-logging-format
    # "INP",   # flake8-no-pep420
    # "PYI",   # flake8-pyi
    # "PT",    # flake8-pytest-style
    # "Q",     # flake8-quotes
    # "RET",   # flake8-return
    # "SLF",   # flake8-self
    # "SIM",   # flake8-simplify
    # "PTH",   # flake8-pathlib
    # "PERF",  # flake8-performance
    # "DOC",   # pydoclint
    # "D",     # docstring checks
    # "PLE",   # pylint-like checks
    # "EM",    # flake8-errmsg
    # "T",     # typing checks
    # "ANN",   # type annotations
    # "Q",     # quality checks

]
ignore = [
    "E722",    # bare except clause
    "B008",    # function calls in argument defaults (common in typer CLI apps)
    "B904",    # raise from within except without explicit exception chaining
    "B007",    # unused loop variables
    "F401",    # unused imports (will clean up separately)
    "C401",    # generator expression improvements
    "C414",    # unnecessary list calls
    "C417",    # unnecessary map usage
    "UP015",   # unnecessary mode arguments
    "W291",    # trailing whitespace
    "PLW0603", # global used in various places
    "S101",    # use of assert in tests
    "E501",    # line too long
]

[tool.ruff.lint.isort]
known-first-party = ["dnd5e"]
section-order = [
    "future",
    "standard-library",
    "third-party",
    "first-party",
    "local-folder",
]
force-single-line = false
combine-as-imports = true
split-on-trailing-comma = true

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
minversion = "7.0"
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--cov=dnd5e",
    "--cov-report=term-missing",
    "--cov-report=html",
    "--cov-report=xml",
]
testpaths = ["tests"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "stress: marks tests as stress tests for validation",
]
filterwarnings = ["ignore::UserWarning", "ignore::DeprecationWarning"]
