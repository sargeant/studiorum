# Configuration for the test suite, loaded through STUDIORUM_CONFIG_FILE.
# content_sources is left at its default: test-data/ and srd-data/ in the
# nearest directory above the working directory that holds both.
data_sources:
  primary_override:
    enabled: false
