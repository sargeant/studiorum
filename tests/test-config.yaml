# Configuration for the test suite, loaded through STUDIORUM_CONFIG_FILE.
# data is left at its default: test-data/ and srd-data/ in the nearest
# directory above the working directory that holds both.
