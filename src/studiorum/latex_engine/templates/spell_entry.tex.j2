% Individual spell entry template using shared spell rendering macro
% Imports the same rendering logic used by spellbook.tex.j2 for consistency

<@ from "_spell_render_block.tex.j2" import render_spell with context @>

<# render_spell(spell, template_service, content_tracker) #>
