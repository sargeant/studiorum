% Book template for D&D 5e adventures and sourcebooks
% Uses dndbook document class with structured content support

<@ extends "base.tex.j2" @>

<@ block document_packages @>
<# super() #>

% Additional packages for book layout
\usepackage{multicol}
\usepackage{wrapfig}
\usepackage{tikz}
\usepackage{tcolorbox}
\usepackage{titlesec}

% Float management for appendices with creatures
\usepackage{afterpage}
\usepackage{placeins}

% DND book-specific environments
\tcbuselibrary{breakable}
<@ endblock @>

<@ block document_config @>
<# super() #>

% Enhanced chapter and section styling for books
% DND-5e-LaTeX-Template provides all styling automatically

% Configure section numbering depth for books
<@ if metadata and metadata.numbering_depth @>
\setcounter{secnumdepth}{<# metadata.numbering_depth #>}
<@ endif @>
<@ endblock @>

<#-- Book content is handled by the modern structured content system in base.tex.j2 --#>
<@ block document_body @>
<#-- Books use chapters which are rendered via the structured content system --#>
<@ endblock @>
