% Individual item entry template using shared item rendering macro
% Imports the same rendering logic used by itemcompendium.tex.j2 for consistency

<@ from "_item_render_block.tex.j2" import render_item @>

<# render_item(item, template_service, content_tracker, entry_processor, rendering_context) #>
